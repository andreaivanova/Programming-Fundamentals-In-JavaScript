function condense (arr){
   //while the array has more than 1 el
   
    // - for every element in the array, sum with next 
    //el(ement), except the last one

    //print

    let condensed = arr;
    let temp = [];
    while(condensed.length > 1){

         for (let i = 0; i < condensed.length - 1; i++) {
           // let temp [];


         }

    }


}
condense([2,10,3])
condense([5,0,4,1,2])
condense([1])