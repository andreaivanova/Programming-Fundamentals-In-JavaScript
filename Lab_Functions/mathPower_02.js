function mathPower(number, power) {
    let result = Math.pow(number, power);
    console.log(result);

    //ALTERNATIVE SOLUTION:
    //let result = 1;
    //  for (let i = 1; i <= x; i++) {
    //     result *= number;
    //  }

}
mathPower(2, 0);
mathPower(3, 4);